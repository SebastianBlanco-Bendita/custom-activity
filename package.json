{
  "name": "gemini-custom-activity",
  "version": "1.0.0",
  "description": "Journey Builder Custom Activity that sends Gemini-powered emails",
  "main": "index.js",
  "scripts": {
    "start": "node -r dotenv/config index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@google/generative-ai": "^0.11.3",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}